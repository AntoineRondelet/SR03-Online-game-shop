#!/bin/bash

# Convert images for the list
# cd assets/images/list && ls | xargs -I '{}' convert '{}' -resize 400x500! '{}'

# Convert images for the preview
# cd assets/images/preview && ls | xargs -I '{}' convert '{}' -resize 750x550! '{}'
